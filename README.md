# Component Library

An interactive component library built with HTML, CSS, and JavaScript.  
Users can click on common HTML elements to learn what they do and how they’re used.

## Getting Started

### Installation
Clone the repository:
```bash
git clone git@github.com:rue-digital/component-library.git
```

### Running Locally
This project uses the Fetch API and must be served over HTTP.
```bash
python3 -m http.server
```
Then open the address printed in the terminal.
